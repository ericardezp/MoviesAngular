export interface CinemaModelDTO {
  cinemaName: string;
  latitude: number;
  longitude: number;
}

export interface CinemaCreateDTO {
  cinemaName: string;
  latitude: number;
  longitude: number;
}
